# NexusOrigin Client Environment Variables
# Copy this file to .env and adjust values

# API base URL for NestJS gateway
VITE_API_BASE=http://localhost:3000/api/v1

# Use mock data instead of real API calls (set to false for production)
VITE_USE_MOCK=true
