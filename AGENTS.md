# 通用AI开发规则

## 一、项目认知
- 项目用途、业务逻辑、交互设计等详细文档统一存放于 `z.about` 文件夹中，执行任何任务前，应先查阅该目录下的相关文档，确保对上下文有充分理解。
- 技术栈与目录结构的完整定义参见 `项目文档.md`，所有开发行为必须与之保持一致。

## 二、开发前必读
1. **每次执行前必须通读本规则文件**，确保所有操作符合规范。
2. 代码修改前，**必须完整通读待修改模块及其依赖的相关定义**（包括实体类、DTO、数据库模型、Prisma Schema 等），严禁在不了解现有实现的情况下盲目修改。
3. 若对字段、类型、关系存在疑问，**优先查阅项目文档**（`z.about` 或 `Product.md`），而非自行猜测。

## 三、编码规范
- **严格遵循现有模型定义**：所有数据库操作必须基于当前 Prisma Schema 中的模型结构，不得添加或删减字段，除非文档明确要求。
- **类型安全**：确保所有 TypeScript 类型声明、接口、断言准确无误，避免出现 `any` 滥用或类型不匹配。
- **语法严谨**：杜绝语法错误、拼写错误、未定义变量、导入缺失等低级问题。
- **勿引入新内容**：现有功能若可复用则优先复用，不可用时才考虑优化或扩展，且扩展前需确认必要性。
- 每完成一次修复都要提交端点，防止后续代码崩溃无法恢复

## 四、输出与提交要求
- 先讨论，输出计划与步骤到对话给我审核，不要args，审核通过后可以开始
- 基于你一次性阅读文件量和输出能力来制定输出计划，内容过长时可以分批次输出，提示我即可

## 五、语言与沟通
- 所有非代码内容（注释、说明、文档、对话）均使用**中文**。
- 每次输出的开头或结尾应简要说明本次修改的目的和影响范围，便于审查。

## 六、错误预防与自查
- 修改后需自行检查：
  - 是否与 Prisma 模型一致
  - 是否与现有 DTO/实体类定义一致
  - 是否存在类型推导错误或隐式 `any`
  - 是否有未处理的异步异常或空值情况
  - 确保修复或优化时，改动部分的涉及文件、组件、前后端逻辑、接口参数传递等内容统一
- 如发现不确定之处，宁可先提问，也不要随意提交不准确的代码。

---

