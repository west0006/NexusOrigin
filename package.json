{
  "name": "shrimptank-platform",
  "private": true,
  "scripts": {
    "dev": "npx rimraf dist && npx nest build && npx nest start --watch",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "postinstall": "cd server/api-gateway/src && npx prisma generate"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.5",
  "dependencies": {
    "tailwindcss": "^4.3.0"
  }
}
